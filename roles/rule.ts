export const DEFAULT_DELEGATION_RULE =
  "## Delegation\nThis thread decides; children do. Read reports and diffs here, never source files, and run no checks. Exploration, research, any change to files and every review go to a fresh child by role, briefed from a file with what its cast line asks for and the cap on what it returns. Every child is a `bb roles spawn`; the provider's own agent or subagent tool stays unused. The coordinator picks each child's reasoning level for the task; the candidate's level is the default. End the turn after a spawn or `bb thread tell`; children notify on completion. A child's question arrives the same way; answer it with `bb thread tell` and end the turn. One unit per builder: one behaviour, about eight files or 400 changed lines, in its own worktree when units run in parallel; a fix round is a fresh builder briefed with the current state. Findings return to the reviewer that raised them with the new head SHA; it answers closed, open or regressed, and this thread decides what blocks. Read back a child's diff and report, never its transcript. Archive a child once its report is read; a reviewer stays until its findings close.";
