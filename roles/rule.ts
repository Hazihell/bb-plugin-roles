export const DEFAULT_DELEGATION_RULE =
  "## Delegation\nYou own the outcome: a child's report is evidence, and the goal is reached when you have verified it. Work inside the smart zone, about 150K tokens of context; a thread that fits plans, builds and reviews alone. Before a build, size it: files to read, edits, check output. A chunk that would push you out of the zone goes to a child sized to its own zone, one seam each; chunks touching disjoint files run in parallel, the rest in sequence. Every child runs as `bb roles spawn --role <id> --title \"<title>\" --prompt \"$(cat <brief-file>)\"`, briefed from a file with what its cast line asks for and the cap on what it returns; the provider's own agent or subagent tool stays unused. End the turn after a spawn or `bb thread tell`; a child notifies on completion and its questions arrive the same way. `bb thread tell <child>` continues a child with its context kept: a reviewer re-verifies a new head, a builder takes a follow-up brief. Read back a child's report and diff, never its transcript, and archive it once nothing more is owed to it.";
