export const DEFAULT_DELEGATION_RULE =
  "## Delegation\nA child runs as `bb roles spawn --role <id> --title \"<title>\" --prompt \"$(cat <brief-file>)\"`, briefed from a file with what its cast line asks for and the cap on what it returns; the provider's own agent or subagent tool stays unused. End the turn after a spawn or `bb thread tell`; a child notifies on completion and its questions arrive the same way. Read back a child's report and diff, never its transcript, and archive it once nothing more is owed to it. `/review` runs the review loop when a preset step or the user asks for it.";
